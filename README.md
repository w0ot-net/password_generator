# password_generator

Generate custom password lists from common patterns (company, seasons, years, city).

## Usage

```bash
python passwords.py --company Acme --output passwords.txt
```

Run `python passwords.py --help` for all options.
